# spotDL Web Application Configuration
# Copy this file to .env and modify the values as needed

# ================================
# AUTHENTICATION SETTINGS
# ================================

# PIN for accessing the application
# WARNING: Change this from the default value for security!
# Use a strong, unique PIN that only you know
PIN=1234

# Session secret key for secure session management
# Generate a random, secure key for production use
# You can use: python -c "import secrets; print(secrets.token_urlsafe(32))"
SESSION_SECRET=your-secret-key-change-this

# ================================
# SERVER SETTINGS
# ================================

# Port for the web application
PORT=8000

# ================================
# NAVIDROME INTEGRATION (Optional)
# ================================

# Your Navidrome server URL
# Example: http://localhost:4533 or https://music.yourdomain.com
SECRET_KEY=default

# Navidrome username
USERNAME=default

# Navidrome password
PASSWORD=passwords

# Navidrome server port (if different from main URL)
# This is used for API calls to Navidrome
NAVIDROME_PORT=4533

# ================================
# SECURITY NOTES
# ================================

# 1. Always change the default PIN before deployment
# 2. Use a strong, random session secret in production
# 3. Keep this file secure and never commit it to version control
# 4. Consider using environment variables instead of this file in production
# 5. Regularly rotate your PIN and session secret

# ================================
# QUICK SETUP COMMANDS
# ================================

# To generate a secure session secret:
# python -c "import secrets; print(secrets.token_urlsafe(32))"

# To load this file in bash:
# export $(cat .env | xargs)

# To run with Docker:
# docker run -p 8000:8000 --env-file .env spotdl-web
